@{
    ViewBag.Title = "Index";
    int index = 1;
}

<div class="container mt-2">
    <div class="card mb-3 shadow-lg">
        <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
            <ol class="carousel-indicators">
                @{ index = 1;}
                @foreach (var item in ViewBag.Post.Imgs)
                {
                    if (index == 1)
                    {
                        <li data-target="#carouselExampleIndicators" data-slide-to="@index" class="active shadow-lg"></li>
                        { index++; }
                    }
                    else
                    {
                        <li data-target="#carouselExampleIndicators" data-slide-to="@index" class="shadow-lg"></li>
                        { index++; }
                    }
                }
            </ol>
            <div class="carousel-inner itemIMGWrapper">
                @{ index = 1;}
                @foreach (var item in ViewBag.Post.Imgs)
                {
                    if (index == 1)
                    {
                        <div class="carousel-item active imgWBlock">
                            <img class="itemIMG d-block" src="@item.href" alt="First slide">
                        </div>
                        { index++; }
                    }
                    else
                    {
                        <div class="carousel-item imgWBlock">
                            <img class="itemIMG d-block" src="@item.href" alt="First slide">
                        </div>
                        { index++; }
                    }
                }
            </div>
            <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
        <div class="card-body">
            <h4 class="card-title"><b>@ViewBag.Post.Name</b></h4>
            <p class="card-text">
                <b>Официальный сайт: </b><a href="@ViewBag.Post.Program.off_site" target="_blank">ссылка</a>
                <br>
                <b>Платформа: </b><a href="@ViewBag.Post.Program.platforms.off_site" target="_blank">@ViewBag.Post.Program.platforms.name</a>
            </p>
            <div class="card-text">
                <b>Описание:</b>
                <div class="description">
                    @Html.Raw(ViewBag.Post.Desc)
                </div>
            </div>
            @{ index = 1;}
            @foreach (var item in ViewBag.Post.Spoilers)
            {
                <div class="accordion spoilerItem" id="accordionExample">
                    <div class="card">
                        <div class="card-header" id="@string.Format("heading{0}", index)">
                            <h5 class="mb-0">
                                <button class="btn btn-link" type="button" data-toggle="collapse" data-target="@string.Format("#collapse{0}", index)" aria-expanded="false" aria-controls="collapseOne">
                                    @Html.Raw(item.name)
                                </button>
                            </h5>
                        </div>
                        <div id="@string.Format("collapse{0}", index)" class="collapse" aria-labelledby="@string.Format("heading{0}", index)" data-parent="#accordionExample">
                            <div class="card-body">
                                @Html.Raw(item.content)
                                @{ index++;}
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
    <div class="card mb-3 text-center shadow-lg">
        <div class="card-header">
            ПОЛЬЗОВАТЕЛЬСКОЕ СОГЛАШЕНИЕ
        </div>
        <div class="card-body">
            <p class="card-text">
                Вы можете <b>использовать</b> эти материалы <b>только</b> в том случае, если использование производится <b>в ознакомительных целях</b>, для прочих целей Вы должны <a href="@ViewBag.Post.Program.off_site" target="_blank">купить лицензию.</a><br />
                Ни при каких обстоятельствах Администрация не несет перед Пользователем, либо третьими лицами ответственности за ущерб, убытки или расходы, возникшие в связи с настоящим сайтом, его использованием или невозможностью использования, включая упущенную либо недополученную прибыль.<br />
                Если Вы не согласны с вышеуказанными условиями, Вы не имеете права использовать сайт.
            </p>
            <a href="@Url.RouteUrl("Default", new {controller = "Item", action = "DownloadFile" })" class="btn btn-success">Скачать</a>
        </div>
        <div class="card-footer text-muted">
            <i>Опубликовано @ViewBag.Post.DatePublic</i>
        </div>
    </div>
    </div>

